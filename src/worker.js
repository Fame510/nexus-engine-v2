const {createWorker}=require('./queue'); const {runJob}=require('./runner'); const {logger}=require('./observability'); const worker=createWorker(job=>runJob(job)); worker.on('completed',job=>logger.info({jobId:job.id},'job completed')); worker.on('failed',(job,error)=>logger.error({jobId:job?.id,error},'job failed')); worker.on('error',error=>logger.error({error},'worker error')); async function shutdown(){logger.info('worker shutting down');await worker.close();process.exit(0);} process.on('SIGTERM',shutdown);process.on('SIGINT',shutdown); logger.info({concurrency:worker.opts.concurrency},'Nexus Engine worker started');
